# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`two-way-sync` is a Python project (requires Python 3.14, pinned via `.python-version`). No dependencies are declared yet.
This is a Python project managed with `uv`. Use `uv run` for execution and `uv add` for dependencies.

## Commands

```bash
# Run the application
uv run main.py

# Install dependencies
uv add <package-name>
```

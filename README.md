## Two-Way-Sync

A two-way sync application for IdeaaS and Quickbooks

### Installation

Mise is recommended for installing and updating the `uv` and `node` binaries.  

```bash
brew install mise
# Then, in the project directory:
mise install
```

### Usage

```bash
uv run main.py
```
